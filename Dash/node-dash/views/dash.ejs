<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard</title>
</head>
<body>
    <h1>Welcome, <%= user.username %>!</h1>
    <p>User ID: <%= user.user_id %></p>
    <p>Acc. Balance: <%= user.account_balance %></p>
    <a href="/logout">Logout</a>

    <h2>Submit a Transaction</h2>
    <form action="/transact" method="POST">
        <label>Amount:</label>
        <input type="number" step="0.01" name="amount" required>
        <button type="submit">Submit Transaction</button>
    </form>

    <% if (transactionResult) { %>
        <div style="margin-top:20px;">
            <% if (transactionResult.prediction !== null) { %>
                <p><strong>Prediction:</strong> <%= transactionResult.prediction %></p>
                <p><strong>Probability:</strong> <%= (transactionResult.probability * 100).toFixed(2) %>%</p>
            <% } %>
            <p><%= transactionResult.message %></p>
        </div>
    <% } %>
</body>
</html>
